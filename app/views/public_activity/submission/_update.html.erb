<%= link_to activity.owner.name, activity.owner if activity.owner %>
<% if activity.trackable %>
  <%= activity.parameters[:status] ? "approved" : "denied" %>
  <%= link_to activity.trackable.content.name, activity.trackable.content %> for the <%= link_to activity.trackable.feed.name, activity.trackable.feed %> feed
<% else %>
  which has since been removed 
<% end %>