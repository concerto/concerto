<%=
    if activity.parameters.include?(:content_type)
      t('.created_a_new_content_with_type', owner: get_activity_owner(activity), item: get_activity_item(activity), content_type: activity.parameters[:content_type])
    else
      t('.created_a_new_content', owner: get_activity_owner(activity), item: get_activity_item(activity))
    end.html_safe
%>
