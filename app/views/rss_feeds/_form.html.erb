<%= form_with(model: rss_feed) do |form| %>
  <% if rss_feed.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rss_feed.errors.count, "error") %> prohibited this rss_feed from being saved:</h2>

      <ul>
        <% rss_feed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="sm:col-span-full">
    <%= form.label :name, class: "frm-label" %>
    <%= form.text_field :name, class: "frm-input" %>
  </div>

  <div class="sm:col-span-full">
    <%= form.label :description, class: "frm-label" %>
    <%= form.text_area :description, class: "frm-input" %>
  </div>

  <div class="sm:col-span-full">
    <%= form.label :url, class: "frm-label" %>
    <%= form.text_field :url, class: "frm-input" %>
  </div>

  <div class="mt-8">
    <%= form.submit class: "btn" %>
  </div>
<% end %>
