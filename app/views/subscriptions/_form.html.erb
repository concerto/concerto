<%= form_with(model: [subscription.screen, subscription]) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :field_id, value: subscription.field_id %>

  <div class="my-2">
    <%= form.label :feed_id, class: "frm-label" %>
    <%= form.collection_select :feed_id, Feed.all, :id, :name %>
  </div>

  <div>
    <%= form.submit class: "btn" %>
  </div>
<% end %>
