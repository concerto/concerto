$("#subscriptions-list").append("<%= escape_javascript(render :partial => 'subscription_tr', :locals => { :subscription => @subscription }) %>");
initializeFrequencySliders();
$('#add-sub-btn').qtip('api').hide();
