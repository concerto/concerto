<%= page_title "New Template" %>

<div class="max-w-7xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Create New Template</h1>
    <p class="mt-1 text-sm text-gray-600">
      Upload a background image and define content areas for your digital signage template.
    </p>
  </div>

  <%= render "form", template: @template %>

  <div class="mt-6">
    <%= link_to "← Back to templates", templates_path, class: "text-blue-600 hover:text-blue-800" %>
  </div>
</div>
