<%= page_title "#{@iframe.name}" %>

<%= render @iframe %>

<%= render "contents/actions",
    content: @iframe,
    content_type: "embedded web page",
    edit_path: edit_iframe_path(@iframe),
    destroy_path: @iframe %>
