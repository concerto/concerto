<div class="max-w-4xl mx-auto">
  <%= form_with(model: iframe, class: "space-y-4") do |form| %>
    <% if iframe.errors.any? %>
      <div class="bg-red-50 border border-red-200 rounded-lg p-3">
        <div class="flex items-start">
          <svg class="w-5 h-5 text-red-400 mt-0.5 mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
          </svg>
          <div class="flex-1">
            <h3 class="text-sm font-semibold text-red-800 mb-2">
              <%= pluralize(iframe.errors.count, "error") %> prohibited this web page from being saved:
            </h3>
            <ul class="text-sm text-red-700 space-y-1">
              <% iframe.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Form Content Card -->
    <div class="bg-white rounded-lg border border-gray-200 shadow-sm">
      <div class="px-4 py-3 border-b border-gray-200">
        <h2 class="text-lg font-semibold text-gray-900">Embedded Web Page</h2>
        <p class="text-sm text-gray-600 mt-1">Display a live web page or dashboard inside an iframe</p>
      </div>

      <div class="p-4 space-y-4">
        <!-- Page URL -->
        <div>
          <%= form.label :url, "Page URL", class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= form.url_field :url,
              class: "block w-full px-3 py-2 border border-gray-300 rounded-lg shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm",
              placeholder: "https://example.com/dashboard" %>
          <p class="text-xs text-gray-500 mt-1">
            Some sites block being embedded (via <code>X-Frame-Options</code> or
            <code>Content-Security-Policy</code>) and will not display.
          </p>
        </div>

        <%= render "contents/form", form: form %>
      </div>
    </div>

    <!-- Form Actions -->
    <div class="flex items-center justify-between pt-2">
      <div class="flex space-x-3">
        <%= link_to "Cancel", contents_path, class: "btn-secondary" %>
      </div>
      <div class="flex space-x-3">
        <%= form.submit "Save Web Page", class: "btn-primary" %>
      </div>
    </div>
  <% end %>
</div>
