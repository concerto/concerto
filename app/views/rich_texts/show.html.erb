<%= page_title "#{@rich_text.name}" %>

<%= render @rich_text %>

<%= render "contents/actions",
    content_type: "rich text",
    edit_path: edit_rich_text_path(@rich_text),
    destroy_path: @rich_text %>
