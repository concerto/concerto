<fieldset>
  <legend><span><%=t(:provide_details)%></span></legend>
  <div class="clearfix">
    <%= form.label :name %>
    <div class="input">
      <%= form.text_field :name %>
    </div>
  </div>

  <%= render partial: 'contents/form_elements/duration', locals: { form: form, min: ConcertoConfig[:min_content_duration].to_i, max: ConcertoConfig[:max_content_duration].to_i} %>
  <%= render partial: 'contents/form_elements/dates', locals: { form: form} %>
</fieldset>

<fieldset>
  <legend><span><%=t(:select_feed)%></span></legend>
  <label>&nbsp;</label>
  <div class="input">
    <% if @content.new_record? %>
      <%= render partial: 'contents/form_elements/feeds' %>
    <% end %>
  </div>
</fieldset>
