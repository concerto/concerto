<%= form_for(@content, :html => { :multipart => true }) do |f| %>
  <% if @content.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@content.errors.count, "error") %> prohibited this content from being saved:</h2>
    <ul>
      <% @content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mime_type %><br />
    <%= f.text_field :mime_type %>
  </div>
  <div class="field">
    <%= f.label :duration %><br />
    <%= f.text_field :duration %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br />
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br />
    <%= f.datetime_select :end_time %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.text_area :data %>
  </div>
  <div class="field">
    <%= f.label :kind %><br />
    <%= f.collection_select :kind_id, Kind.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.collection_select :user_id, User.all, :id, :name %>
  </div>
  <% if @content.new_record? %>
  <div class="field">
    <% dfs_tree(Feed.roots).each_with_index do |feed, i| %>
      <%= check_box_tag "feed_id[#{i}]", feed.id, @content.feeds.include?(feed) %>
      <%= "-" * feed.depth %> <%= feed.name %><br />
    <% end %>
  </div>
  <% end %>
  <div class="field">
   <%= f.fields_for :medias do |media| %>
    <%= media.file_field :file %>
   <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
