// alerts-badges.scss.erb

.badge {
  @include border-radius(2px);
  background: $concerto-blue-4;
  color: $white;
  font-family: $sans-body;
  display: inline;
  font-size: 10px;
  font-weight: bold;
  padding: 1px 4px;
}

.link-hl .badge {
  position: relative;
  bottom: 2px;
  margin-right: 3px;
}
