en:
  activerecord:
    attributes:
      screen:
        name: 'Name'
        location: 'Location'
        owner: 'Owner'
        owner_id: 'Owned By'
        is_public: 'This screen is publicly viewable'

  screens:
    index_body:
      add_one: 'Why not add one?'
      my_screens: 'My Screens'
      no_screens: 'Oops - there are no screens to display.'
      other_screens: 'Other Screens'
      to_add: " to add one"

    feeds:
      filter_for_feeds: "Filter for Feeds"
      select_feed: "Select Feed"

    form:
      best_fit_templates: "Best-Fit Templates"
      dimensions: 'Dimensions'
      provide_details: 'Provide Details'
      select_template: 'Select Template'
      authentication: 'Authentication'
      connection_details: 'Connection Details'
      auth_action: 'Authentication Action'
      no_auth: 'No authentication (unsecured)'
      new_token: 'New token for a secure setup'
      private_token: 'Secured by existing private token'
      new_token_override: 'Secure with a new token (removing the existing authentication)'
      existing_mac: 'Existing legacy MAC Address'
    thumbs:
      owner: 'owner'

      #current_template: "Current Selection"
      #other_templates: "Other Templates"
