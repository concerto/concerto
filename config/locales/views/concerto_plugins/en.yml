en:
  activerecord:
    models:
      concerto_plugin: 'Plugin'
    attributes:
      concerto_plugin:
        source: 'Source'
        gem_name: 'Gem Name'
        source_url: 'Source URL'

  concerto_plugins:
    sources:
      rubygems: 'RubyGems'
      git: 'Git Repository'
      path: 'Filesystem Path'

    index:
      disabled: "disabled"
      enabled: "enabled"
